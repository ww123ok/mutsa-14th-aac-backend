spring:
  datasource:
    url: jdbc:h2:mem:dev_diary_reset_test;MODE=MySQL;DB_CLOSE_DELAY=-1
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create-drop

app:
  openai:
    api-key: test-key
    reflection-enabled: false
    reward-enabled: false
    memory-extraction-enabled: false

  weekly-reward:
    enabled: false
